// The Life — connection settings.
//
// Fill these in with your own Supabase project (Project Settings → API):
//   SUPABASE_URL      "Project URL" only — https://<ref>.supabase.co,
//                     with no /rest/v1 or other path after it
//   SUPABASE_ANON_KEY the public client key: "Publishable key" (sb_publishable_...)
//                     or, from the Legacy tab, the "anon public" key (eyJ...)
//
// Both values are safe to publish: the anon key only works together with the
// row level security rules from supabase/schema.sql, which let a person read
// and write exactly the boards they are a member of.

window.THE_LIFE_CONFIG = {
  SUPABASE_URL: "https://crsmuawtgnqiwxveyiuv.supabase.co",
  SUPABASE_ANON_KEY: "sb_publishable_pFWMUB2XbVfAYc7Qp2YcKA_U4mIAXrx"
};
