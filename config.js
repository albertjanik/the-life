// The Life — connection settings.
//
// Fill these in with your own Supabase project (Project Settings → API):
//   SUPABASE_URL      "Project URL"      e.g. https://abcdefghijklm.supabase.co
//   SUPABASE_ANON_KEY "anon public" key  — a long string starting with "eyJ..."
//
// Both values are safe to publish: the anon key only works together with the
// row level security rules from supabase/schema.sql, which let a person read
// and write exactly the boards they are a member of.

window.THE_LIFE_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
};
